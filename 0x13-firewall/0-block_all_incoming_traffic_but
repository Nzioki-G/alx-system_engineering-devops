# block all incoming traffic
sudo ufw default deny incoming

# allow ssh
sudo ufw allow ssh
sudo ufw allow 22

# allow HTTPS SSL
sudo ufw allow 443

#allow HTTP
sudo ufw allow 80

# restart the firewall to save changes
sudo ufw enable
